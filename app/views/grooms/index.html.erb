<h1>Listing grooms</h1>

<table>
  <tr>
    <th>Grappt date</th>
    <th>String</th>
    <th>Grappt futappdate</th>
    <th>String</th>
    <th>Grappt style</th>
    <th>String</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @grooms.each do |groom| %>
  <tr>
    <td><%= groom.grappt_date %></td>
    <td><%= groom.string %></td>
    <td><%= groom.grappt_futappdate %></td>
    <td><%= groom.string %></td>
    <td><%= groom.grappt_style %></td>
    <td><%= groom.string %></td>
    <td><%= link_to 'Show', groom %></td>
    <td><%= link_to 'Edit', edit_groom_path(groom) %></td>
    <td><%= link_to 'Destroy', groom, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Groom', new_groom_path %>
